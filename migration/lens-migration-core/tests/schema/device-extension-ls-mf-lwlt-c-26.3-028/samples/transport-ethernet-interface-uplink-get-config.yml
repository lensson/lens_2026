arguments:
  network-port-name:
    description: Name of interface
    suggested-values:
    - X1/X2
available-locally: true
domains: transport>ethernet
entity: interface-uplink
freemarker: true
name: Retrieve running configuration on interface
operation: get-config
sample-id: transport-ethernet-interface-uplink-get-config
scope: not(inband-no-ipv4) and not(mf2-nt) and not(mx) and not(dpu)
status: draft
