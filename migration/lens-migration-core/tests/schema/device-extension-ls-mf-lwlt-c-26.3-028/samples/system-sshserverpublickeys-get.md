This macro handles the ability of an administrator to get information of the installed public-keys of SSH server key-pair

Input parameters:

* host-key-name: The name of the algorithm to display the public key

