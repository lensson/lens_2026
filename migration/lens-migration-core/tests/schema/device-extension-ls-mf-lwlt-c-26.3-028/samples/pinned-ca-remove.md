This macro deletes the pinned (CA) certificates.

Pre-condition: the pinned-certificates list is not used (as reference) by a feature.
Input parameters:

* certificate-list-name: Certificate list name

