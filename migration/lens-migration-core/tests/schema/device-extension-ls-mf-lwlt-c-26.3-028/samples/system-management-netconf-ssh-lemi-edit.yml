arguments:
  netconf-ssh-lemi-enabled:
    description: Enables (True) or disables (False) NETCONF access over SSH via LEMI
      interface
    suggested-values:
    - true
available-locally: true
domains: system>management
entity: netconf-ssh-lemi
freemarker: true
name: Configure enabling of NETCONF/SSH via LEMI
operation: edit
sample-id: system-management-netconf-ssh-lemi-edit
scope: not(fx-nt) and not(fx-p2p)
status: draft
