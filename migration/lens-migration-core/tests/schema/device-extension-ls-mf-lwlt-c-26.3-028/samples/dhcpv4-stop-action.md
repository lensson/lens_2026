This macro performs the DHCPv4 Client session simulation stop action.

Prerequisite:

* The dhcpv4-client-simulator session interface needs to be configured.
* DHCPv4 client session simulation start action to be triggered/performed.

Input parameters:

* interface: The user vlan-sub-interface on which the dhcp client simulation is to be performed

