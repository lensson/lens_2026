This macro removes the log levels for an application.
Reset will revert the logging levels of all submodules of the specific application to default WARNING level.

Pre-condition:

* To be able to configure logging level of a specific application you should explicit use the application and module name as returned by active-applications.

Input parameters:

* app-name: application name

