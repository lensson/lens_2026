This macro retrieves RADIUS session state and statistics.

Note:

* Radius authentication Server session state and statistics supported for both 802.1X user authentication and operator authentication.

* Radius accounting Server session state and statistics supported only for 802.1X user authentication.

