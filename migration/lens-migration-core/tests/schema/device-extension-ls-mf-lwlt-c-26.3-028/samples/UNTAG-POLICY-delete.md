This macro deletes a QoS untag policy along with classifier.

* a policy used for untag is deleted.
* a classifier used for setting default pbit is deleted.

Pre-condition:

* OLT device has to be created.

Input parameters:

* untag-policy-name: Policy name

