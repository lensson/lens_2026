This macro deletes the  ptp interface configuration data . 

* every onu owns its own ptp profile

Input parameters:

* onu-name: the custom name of the onu.
* uni-name: the custom name of the uni interface.

