This macro removes Dynamic Authorization Server listen ports configuration.

Prerequisite:

* Dynamic Authorization Server listen ports configuration must exist.

Input parameters:

* listen-ports: Dynamic Authorization Server(DAS) shall start listening on the configured UDP ports to handle dynamic Authorization requests from the Dynamic Authorization Client(DAC)

