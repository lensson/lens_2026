This macro retrieves the operational data of IPFIX caches on the LightSpan.
It reports the names of the configured caches, along with their template ids,
and the latest discontinuity time (if any).
For each cache the number of instances that have been exported as well as the number of 
stored-data-records, dropped-data-records and total-dropped-data-records are reported.

