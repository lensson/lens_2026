This macro deletes a QoS policing policy along with classifier/policer.

* a policy profile used for policing is deleted.
* a classifiers used for ratelimit is deleted.

Pre-condition:

* OLT device has to be created.

Input parameters:

* policing-policy-name: Policy name

