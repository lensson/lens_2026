This macro deletes a configured IPFIX template and associated attributes. The data belonging to this template are ceased from exporting immediately after successful removal.
Input parameters:

* cache-name: cache name

