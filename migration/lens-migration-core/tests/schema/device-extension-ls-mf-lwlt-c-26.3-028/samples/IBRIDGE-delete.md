This macro deletes an N:1-VLAN Forwarder, its associated forwarding database and the attached network interface.

* The macro deletes the network VLAN sub-interface
* The N:1 VLAN Forwarder is deleted.
* The forwarding database is also deleted.

Input parameters:

* ibridge-name: the name of the IBRIDGE

