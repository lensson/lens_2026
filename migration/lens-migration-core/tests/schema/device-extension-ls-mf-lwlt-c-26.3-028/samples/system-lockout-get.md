This macro get information on which users/ips/system are in lockout

Pre-condition:

* Admin user can retrieve the data for the users in lockout
* Remaining time until lockout is also displayed
* In case of global lockout, if administrator is already connected, it   will be possible to check if global lockout happens in the system.

