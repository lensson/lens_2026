This macro removes all configured destinations (remote and local).

* Remove syslog configuration.
* If a local log file is configured, then the file itself and all rotated files are removed from the local filesystem. Please make sure to retrieve all the affected log files from the device before proceeding to such a configuration.

