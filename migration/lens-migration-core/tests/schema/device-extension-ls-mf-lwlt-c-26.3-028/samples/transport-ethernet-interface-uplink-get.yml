arguments:
  network-port-name:
    description: Name of interface
    suggested-values:
    - X1/X2
available-locally: true
domains: transport>ethernet
entity: interface-uplink
freemarker: true
name: Retrieve interfaces state for interface
operation: get
sample-id: transport-ethernet-interface-uplink-get
scope: not(inband-no-ipv4) and not(mf2-nt) and not(dpu) and not(mx)
status: draft
