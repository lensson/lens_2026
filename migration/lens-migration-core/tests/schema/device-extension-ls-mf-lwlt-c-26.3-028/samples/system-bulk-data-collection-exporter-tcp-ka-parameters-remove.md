This macro removes the TCP Keep-Alive configuration for the connection to the IPFIX Collector. If the parameters are not configured the default TCP Keep-Alive (i.e., idle time 7200secs) is applied.

Input parameters:

* exporter-name: exporter name

