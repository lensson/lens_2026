This macro enables/disables NETCONF over SSH via LEMI
Precondition for enablement is that operator LEMI access has been enabled.

Input parameters:

* netconf-ssh-lemi-enabled: Enables (True) or disables (False) NETCONF access over SSH via LEMI interface

