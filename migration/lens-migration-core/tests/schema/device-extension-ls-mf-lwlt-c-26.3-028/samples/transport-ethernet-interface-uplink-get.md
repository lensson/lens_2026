This macro displays the interface state for a given Uplink interface.

Pre-condition:

* SFP should be plugged at the the UPLINK cage
* Create uplink interface using macro "NETWORK-PORT-create"
* Uplink interface should be enabled using "NETWORK-PORT-enable"

Input parameters:

* network-port-name: Name of interface

