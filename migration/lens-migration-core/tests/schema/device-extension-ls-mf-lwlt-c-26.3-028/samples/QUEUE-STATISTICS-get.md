This macro retrieves qos statistics on the scheduler node.

Pre-condition:

* the named scheduler node exists.
* the queues created on the named scheduler node.
* the qos performance and statistics enabled.

Input parameters:

* olt-infrastructure-name: 
* queue-id: 
* scheduler-name: 

