This macro deletes an GPON port infrastructure in the OLT which can be used as underlying hardware for a channel-termination interface in an GPON OLT infrastructure.

Input parameters:

* olt-port-name: OLT infrastructure port name.

