This macro creates a CPU packet rate limit policy.

* If both of the policy and classifier with the specified name did not exist before, then they are created.
* If one the them with the specified name did exist before, then modify it.

Pre-condition: the policy and classifier entry (see further) shall not already exist.

Input parameters:

* burst-size: 
* packet-rate-policy-profile-name: 
* rate: 

