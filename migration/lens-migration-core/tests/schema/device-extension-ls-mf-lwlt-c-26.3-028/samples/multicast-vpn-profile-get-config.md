
* This macro retrieves the multicast VPN profile configuration.

Prerequisites:

* No prerequisites.

Input parameters:

* detailed-output: Provides detailed configuration of all the multicast proxy profiles in the system when enabled and a list of configured multicast proxy profile names when disabled. This argument will be ignored when the vpn-name is provided.
* vpn-profile-name: The name of the multicast proxy profile.

