This macro sets the downstream broadcast flag to the forwarder attached with DHCPv4 Relay Agent profile.

Input parameters:

* bc-flooding: Downstream broadcast DHCP packets are forwarded according forwarding rules configured for the forwarder
* cc-name: The name of CC sample
* ibridge-name: Ibridge user port name

