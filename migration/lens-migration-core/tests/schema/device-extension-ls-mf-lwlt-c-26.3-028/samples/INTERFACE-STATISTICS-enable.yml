arguments:
  interface-statistics-mode:
    allowed-values:
    - best-effort
    - 'false'
    optional: true
    suggested-values:
    - best-effort
  venet-interface-name:
    description: the name of the olt-v-enet who will enable interface statistics.
    suggested-values:
    - VSI
domains: unicast>forwarding
entity: interface-statistics
freemarker: true
name: Enable statistics on OLT-vENET interface
operation: enable
sample-id: unicast-forwarding-interface-statistics-enable
scope: not(mx) and not(fx-p2p)
status: draft
