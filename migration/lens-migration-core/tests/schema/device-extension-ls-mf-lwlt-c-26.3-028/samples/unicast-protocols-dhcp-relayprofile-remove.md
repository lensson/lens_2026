This macro deletes the DHCPv4 Relay Agent profile.

Pre-condition:

* DHCPv4 Relay Agent profile must exist before deleting.
* DHCPv4 Relay Agent profile should not be attached to any VLAN sub-interface.

Input parameters:

* dhcp-profile-name: Name of the DHCPv4 Relay Agent profile

