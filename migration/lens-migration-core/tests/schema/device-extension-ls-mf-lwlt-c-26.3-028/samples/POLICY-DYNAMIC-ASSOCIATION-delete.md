This macro disables a VLAN sub-interface for dynamic qos policy association through 802.1x COA.

Pre-condition:

* VLAN sub-interface should exist with interface-usage as user-port.

Input parameters:

* vsi-interface-name: The name of the VLAN sub-interface where qos-policy-dynamic-association will be deleted

