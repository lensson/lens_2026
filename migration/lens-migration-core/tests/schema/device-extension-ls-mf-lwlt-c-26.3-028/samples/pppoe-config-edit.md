This macro configures user VLAN sub-interface to pppoe-client-simulator as a pre-requisite to start the PPPoE client simulator.

Input parameters:

* interface: The user vlan-sub-interface on which the PPPoE Client simulation is to be performed

