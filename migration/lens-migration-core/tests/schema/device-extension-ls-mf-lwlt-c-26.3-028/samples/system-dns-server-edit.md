This macro configures a DNS server.

Input parameters:

* dns-server-address: Both IPv4 and IPv6 addresses are supported.
* dns-server-name: An arbitrary name for the DNS server
* dns-server-port: The port number of DNS server

