Get-config banners.

Pre-condition:

* A welcome banner can be configured in the device and it can be shown to the user when the CLI is started. Default is empty. This configuration parameter takes effect only for new sessions.
* A login banner is a string that will be presented to the client before authenticating when logging in to the CLI via the built-in SSH server.

