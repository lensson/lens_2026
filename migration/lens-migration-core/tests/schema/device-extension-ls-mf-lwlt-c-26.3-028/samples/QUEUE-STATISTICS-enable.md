This macro enables qos performance and statistics on the scheduler node.

Pre-condition:

* the named scheduler node exists.
* the queues created on the named scheduler node.

Input parameters:

* enable: 
* olt-infrastructure-name: 
* scheduler-name: 

