This macro removes a TLS version or ciphers from the relevant lists of hello-params.
Input parameters:

* cipher-suite: YANG identifier of a TLS cipher to add
* tls-version: YANG identifier of a TLS protocol version to add

