This macro deletes an intermediate scheduler node that passes traffic to another scheduler node in a hierarchy.

Pre-condition:

* The named scheduler node exists.

Input parameters:

* intermediate-scheduler-name: 
* olt-infrastructure-name: 

