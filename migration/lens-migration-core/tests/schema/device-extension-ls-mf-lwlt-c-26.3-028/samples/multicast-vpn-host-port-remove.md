Delete multicast host port for multicast vpn.

Prerequisites:

* The multicast vpn must exist.
* The multicast host port must exist.

Input parameters:

* vpn-host-port-name: The name of a multicast host port.
* vpn-name: The name of a multicast vpn.

