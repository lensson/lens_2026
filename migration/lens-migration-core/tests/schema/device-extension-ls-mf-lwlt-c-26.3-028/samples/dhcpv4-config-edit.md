This macro configures user VLAN sub-interface to dhcpv4-client-simulator as a pre-requisite to start the DHCPv4 client simulator.


Input parameters:

* interface: The user vlan-sub-interface on which the dhcp client simulation is to be performed

