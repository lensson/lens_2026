arguments:
  cc-name:
    description: The name of the cross-connect.
    suggested-values:
    - cc-1
  onu-name:
    description: ONU name
    optional: true
    suggested-values:
    - ani-1-1-1
  onu-template-cc-name:
    description: ONU template CC interface name
    optional: true
    suggested-values:
    - cc-template-1
domains: unicast>forwarding
entity: cc
freemarker: true
name: Retrieve unicast services forwarding cc config data
operation: get-config
sample-id: unicast-forwarding-cc-get-config
scope: not(mx) and not(dpu) and not(fx-p2p)
status: draft
