Get-config a specific group.

Pre-condition:

* Only user belongs to admin group can dump the users of a group.

Procedure:

* An admin user can retrieve the users which are assigned to a specific group.


Input parameters:

* group-name: Group name associated with this entry.
* user-name: User name to be found in group.

