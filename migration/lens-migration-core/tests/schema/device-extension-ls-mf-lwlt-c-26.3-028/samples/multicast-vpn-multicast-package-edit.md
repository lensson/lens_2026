
* This macro creates/modifies the multicast package.

Prerequisite:

* The multicast vpn must exist.

Input parameters:

* is-preview: Indicates the multicast channels of this package can be joined as controlled by the preview configuration data or not.
* multicast-package-name: The name of a multicast package.
* vpn-name: The name of a multicast vpn.

