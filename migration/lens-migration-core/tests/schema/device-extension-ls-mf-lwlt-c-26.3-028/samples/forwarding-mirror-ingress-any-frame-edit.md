This macro configures mirror ingress any-frame filter to the input vlan-sub-interface.

Input parameters:

* input-interface: Input VSI that configures the mirror ingress any-frame filter.
* output-interface: References the output VLAN sub-interface.

