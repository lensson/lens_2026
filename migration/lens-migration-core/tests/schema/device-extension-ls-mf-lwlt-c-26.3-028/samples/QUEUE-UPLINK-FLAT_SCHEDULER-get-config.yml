arguments:
  uplink_name:
    suggested-values:
    - UPLINK1
domains: unicast>qos>tm
entity: queue_uplink_flat_scheduler
freemarker: true
name: Retrieve unicast services QoS queue for tm-root
operation: get-config
sample-id: unicast-qos-tm-queue_uplink_flat_scheduler-get-config
scope: not(mx) and not(dpu) and not(fx-p2p)
status: draft
