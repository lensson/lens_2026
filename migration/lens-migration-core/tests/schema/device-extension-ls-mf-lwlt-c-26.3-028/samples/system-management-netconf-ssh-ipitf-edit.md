This macro enables/disables NETCONF over SSH via configured IPforward interface.

Input parameters:

* netconf-ssh-ipitf-enabled: Enables (True) or disables (False) NETCONF access over SSH via IPforward interface

