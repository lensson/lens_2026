This macro configures Authentication server at system level.

Set the 802.1X Authentication server as RADIUS Server.

Input parameters:

* authentication-server: Indicates that a user will be authenticated using radius communication to a remote server.

