This macro creates the interface speed monitoring which defines the period length at system level.
If not configured, the default value is 15-minutes. The device will collect data every 15 minutes.

Input parameters:

* period-length: the monitoring period over which the data will be collected.

