This macro retrieves the ONU SyncE/SSM status.

* It's only supported for those ONUs which support G.988 standard ME464 'Synchronous Ethernet operation' when SyncE/SSM function is unlocked.

Input parameters:

* onu-name: the custom name of the onu.

