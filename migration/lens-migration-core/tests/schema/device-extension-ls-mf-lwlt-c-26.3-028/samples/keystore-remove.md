This macro removes the asymmetric-key list factory-callhome-key from the centralized asymmetric-keys list.

Pre-condition: the asymmetric-key list is not used (as reference to keystore) by a feature.

