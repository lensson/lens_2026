This macro removes a remote syslog collector.

Pre-condition:

* A remote log collector to have been configured before.

Description:

* In the following example the rule for the specific remote collector is removed from the device.
* The syslog messages will no longer be forwarded from the device to the previously configured remote collector.

Input parameters:

* syslog-collector-name: Remote destination log collector name

