This macro deletes a leaf scheduler node.

Pre-condition:

* the channel-partition interface must exist
* leaf-scheduler-name must exist

Input parameters:

* intermediate-scheduler-name: 
* leaf-scheduler-name: 
* olt-infrastructure-name: 

