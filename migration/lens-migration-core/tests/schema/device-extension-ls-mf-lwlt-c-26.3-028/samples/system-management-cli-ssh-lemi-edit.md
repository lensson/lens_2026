This macro enables/disables CLI access over SSH via LEMI.
Precondition for enablement is that operator LEMI access has been enabled.

Input parameters:

* cli-ssh-lemi-enabled: Enables (True) or disables (False) CLI access via LEMI

