This macro reestablishes the call-home connection. It triggers call-home restart to establish call-home connection to PMA with configured key/certificates.

Note: If call-home established successfully with above certificates, those  will be stored to the device's persistent storage.
