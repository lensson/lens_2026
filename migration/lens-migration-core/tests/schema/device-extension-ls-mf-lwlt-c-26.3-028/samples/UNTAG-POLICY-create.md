This macro creates a QoS untag policy along with classifier.

* a policy used for untag is created.
* a classifier used for setting default pbit is created.

Pre-condition:

* OLT device has to be created.

Input parameters:

* default-pbit: the default pbit value for untagged packets
* untag-policy-name: Policy name

